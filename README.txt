Drop your two logo files here, named exactly:
  logo.png   -> Aftenposten logo (shown in the left column)
  logo1.png  -> Morgenbladet logo (shown in the right column)

Any image format works if you also update the src="" in index.html
(currently assets/logo.png and assets/logo1.png). PNG with a
transparent background looks best. If a file is missing, that logo
slot just stays empty — nothing breaks.
